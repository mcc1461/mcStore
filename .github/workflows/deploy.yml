name: Deploy Frontend to Softrealizer

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "23" # use node version compatible with your app

      - name: Install Dependencies and Build
        working-directory: ./client
        run: |
          npm install
          npm run build

      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.AWS_MUSCO_STORE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            cd ~/mcStore/client

            # Reset and fetch the latest code
            git reset --hard HEAD
            git clean -fd
            git pull origin main

            # Install dependencies and build on the server
            npm install
            npm run build

            # Debug: list contents of the new build folder
            echo "New build (dist/) contents:"
            ls -la dist

            # Remove old build files from the document root
            sudo rm -rf /var/www/softrealizer/*
            sudo rm -rf /var/www/softrealizer/.*

            # Copy new build using rsync to ensure an exact mirror of dist/
            sudo rsync -av --delete dist/ /var/www/softrealizer/

            # Set proper ownership for nginx to serve the files
            sudo chown -R www-data:www-data /var/www/softrealizer

            # Debug: list contents of the deployment folder
            echo "Updated /var/www/softrealizer contents:"
            ls -la /var/www/softrealizer

            # Reload nginx and restart the Node.js app
            sudo systemctl reload nginx
            pm2 restart all
